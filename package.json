{
  "name": "inkroom-app",
  "version": "1.0.0",
  "description": "Color Quality Management System for Printing Processes",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:preview": "wrangler deploy --env preview",
    "deploy:preview2": "wrangler deploy --env preview2",
    "deploy:preview3": "wrangler deploy --env preview3",
    "deploy:preview4": "wrangler deploy --env preview4",
    "cf-typegen": "wrangler types",
    "test": "playwright test"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "hono",
    "printing",
    "color-management"
  ],
  "author": "Markus Ketzer",
  "license": "MIT",
  "dependencies": {
    "hono": "^4.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "wrangler": "^4.81.1"
  }
}
